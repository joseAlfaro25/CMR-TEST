export interface RawContact { id: string; organization_id?: string | null; full_name?: string | null; phone?: string | null; email?: string | null; lead_source?: string | null; contact_type?: string | null; created_at?: string | number | null; notes?: string | null; tags?: string[] | null; qualification_data?: unknown; interactions?: RawInteraction[] | null; [key: string]: unknown; }
export interface RawInteraction { id: string; channel?: string | null; direction?: string | null; created_at?: string | number | null; content?: string | null; metadata?: Record<string, unknown> | null; }
export type FactSource = 'human' | 'ai' | 'import' | 'unknown';
export interface QualificationFact { key: string; label: string; value: unknown; source: FactSource; sourceLabel: string; date: string | null; sourceRef: string | null; history?: { value: unknown; sourceLabel: string; date: string | null }[]; }
export interface InteractionViewModel { id: string; channel: string; channelLabel: string; direction: string; date: string | null; dateValue: number; content: string; transcript: string | null; }
export interface ContactViewModel { id: string; organizationId: string | null; name: string; initials: string; phone: string | null; phoneDigits: string | null; email: string | null; origin: string; originLabel: string; createdAt: string | null; createdValue: number; notes: string | null; tags: string[]; qualification: Record<string, QualificationFact[]>; interactions: InteractionViewModel[]; compliance: { restricted: boolean; phoneBlocked: boolean; whatsappBlocked: boolean; reason: string | null }; duplicateOf: { id: string; name: string; reasons: string[] }[]; }
